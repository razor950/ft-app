## Server Properties
server.port= 5000

## Spring DATASOURCE
spring.datasource.url= jdbc:mysql://localhost:3306/foodtruck_app?useSSL=false&serverTimezone=UTC&useLegacyDatetimeCode=false
spring.datasource.username= root
spring.datasource.password= agents007

## Hibernate Properties
# The SQL dialect makes Hibernate generate better SQL and ddl-auto in update allows for tables to get added/removed based on models
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5InnoDBDialect
spring.jpa.hibernate.ddl-auto = update

## Hibernate Logging
logging.level.org.hibernate.SQL= DEBUG

## Jackson Properties
spring.jackson.serialization.WRITE_DATES_AS_TIMESTAMPS= false
spring.jackson.time-zone= UTC

## App Properties
app.jwtSecret= JWTSuperSuperSuperSecretKey
app.jwtExpirationInMs = 604800000
